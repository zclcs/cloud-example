<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zclcs.platform.system.mapper.UserDataPermissionMapper">

    <sql id="baseSql">
        select sudp.username,
               sudp.dept_code,
               su.username,
               su.password,
               su.email,
               su.mobile,
               su.status,
               su.last_login_time,
               su.gender,
               su.is_tab,
               su.theme,
               su.avatar,
               su.description,
               sd.parent_code,
               sd.dept_name,
               sd.order_num
        from system_user_data_permission sudp
                 inner join system_user su on sudp.username = su.username
                 inner join system_dept sd on sudp.dept_code = sd.dept_code
            ${ew.customSqlSegment}
    </sql>
    <sql id="baseCountSql">
        select count(*)
        from system_user_data_permission sudp
                 inner join system_user su on sudp.username = su.username
                 inner join system_dept sd on sudp.dept_code = sd.dept_code
            ${ew.customSqlSegment}
    </sql>
    <select id="findPageVo" resultType="com.zclcs.platform.system.api.bean.vo.UserDataPermissionVo">
        <include refid="baseSql"/>
    </select>
    <select id="findListVo" resultType="com.zclcs.platform.system.api.bean.vo.UserDataPermissionVo">
        <include refid="baseSql"/>
    </select>
    <select id="findOneVo" resultType="com.zclcs.platform.system.api.bean.vo.UserDataPermissionVo">
        <include refid="baseSql"/>
    </select>
    <select id="countVo" resultType="java.lang.Integer">
        <include refid="baseCountSql"/>
    </select>
</mapper>
