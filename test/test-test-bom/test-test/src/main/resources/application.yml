server:
  port: ${PORT_TEST_TEST:9101}
spring:
  profiles:
    active: ${RUN_MODE:local}
  application:
    name: test-test
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_HOST:192.168.33.10}:${NACOS_PORT:8848}
        namespace: ${NACOS_NAMESPACE:dev}
        group: DEFAULT_GROUP
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:nacos}
      discovery:
        server-addr: ${spring.cloud.nacos.config.server-addr}
        namespace: ${spring.cloud.nacos.config.namespace}
        group: DEFAULT_GROUP
        username: ${spring.cloud.nacos.config.username}
        password: ${spring.cloud.nacos.config.password}
  config:
    import:
      - nacos:global-configuration.yaml
      - nacos:tomcat.yaml
      - nacos:openfeign.yaml
      - nacos:hikari.yaml
      - nacos:rabbitmq.yaml
      - nacos:redis.yaml
      - nacos:mybatis-plus.yaml
      - nacos:jasypt.yaml
      - nacos:system-dict.yaml
      - nacos:minio.yaml
      - nacos:db-merge.yaml
      - nacos:sa-token.yaml

  aop:
    proxy-target-class: true

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  datasource:
    driver-class-name: ${DATASOURCE_DRIVER:com.mysql.cj.jdbc.Driver}
    url: jdbc:${DATASOURCE_DB_TYPE:mysql}://${DATASOURCE_HOST:192.168.33.10}:${DATASOURCE_PORT:3306}/${spring.cloud.nacos.config.namespace}_test_test?${DATASOURCE_PARAM:useUnicode=true&characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&zeroDateTimeBehavior=convertToNull&rewriteBatchedStatements=true&serverTimezone=Asia/Shanghai}
    username: ${DATASOURCE_USERNAME:root}
    password: ${DATASOURCE_PASSWORD:root}

mybatis-plus:
  type-aliases-package: com.zclcs.test.test.api.entity
  mapper-locations: classpath:com/zclcs/test/test/biz/mapper/*.xml

# spring security 配置
security:
  oauth2:
    client:
      client-id: ENC(imENTO7M8bLO38LFSIxnzw==)
      client-secret: ENC(i3cDFhs26sa2Ucrfz2hnQw==)
      scope: server
    # 通用放行URL，服务个性化，请在对应配置文件覆盖
    ignore:
      urls:
        - /v3/api-docs